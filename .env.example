LLM_API_KEY=
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=openai/gpt-5.4

# Backward-compatible OpenRouter names are still supported.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=openai/gpt-5.4

# Required only for HR retrieval/workflow runs in llm mode.
OPENROUTER_EMBEDDING_MODEL=

# Optional; defaults to backend/data/faiss for persisted HR vector indexes.
PREPPER_HR_VECTOR_STORE_DIR=

# Optional backend in-memory cleanup settings; defaults are 86400 seconds and 1000 entries.
PREPPER_STATE_TTL_SECONDS=
PREPPER_STATE_MAX_ENTRIES=

# Optional dev override. Keep unset/false to block private/internal company URL fetches.
PREPPER_ALLOW_PRIVATE_URL_FETCH=

# Documentation-only default for HR prototype examples; CLI/API flags still choose mode.
HR_SIMULATION_MODE=mock

PREPPER_DEFAULT_SYSTEM_PROMPT=coding_focus
